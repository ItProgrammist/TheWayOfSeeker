extends Node

signal seeker_position_update(seeker_pos)

signal enemy_position_update(enemy_pos)

signal enemy_attack(enemy_damage, enemy_pos)

signal player_attack(player_damage, player_pos)

signal enemy_died(enemy_position)
